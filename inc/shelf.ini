shelf_write () 
{ 
    set ~/git/shelf/inc;
    local file=$(which shelflib);
    set $1/*.fam $1/util.all $1/shelf.ini;
    cat $* | onlyChanged $file;
    chmod +x $file;
    source $file
}
shelf_init ()
{
     fam_init
     report_init
     shdoc_init
     trace_init
}
shelf_init 1>&2
